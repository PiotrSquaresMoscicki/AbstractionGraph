{
    "name": "abstraction-graph",
    "version": "1.0.0",
    "description": "A TypeScript project called abstraction-graph",
    "scripts": {
        "build": "tsc",
        "start": "node dist/index.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "vite",
        "build-vite": "tsc --version && tsc && vite build --base ./",
        "preview": "vite preview"
    },
    "dependencies": {
        "@maxgraph/core": "^0.6.0",
        "typescript": "^4.4.3"
    },
    "devDependencies": {
      "vite": "~4.4.12"
    }
}
